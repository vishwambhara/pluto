store.host="localhost"
store.port=27017
store.dbName="pluto-event-store"

//the following should be true if the events are not generated by this application
//readOnly=true
// For replication
//replication.run=true
//replication.autoUpdateMinInterval=5000
//replication.autoUpdateMaxInterval=60000
//replication.autoUpdateStepInterval=2000
//replication.autoUpdateReplayMax=100
//the following are for replication related properties
//above readOnly must be true for replication
//source.store.host="remote-host"
//source.store.port=27017
//source.store.dbName="pluto-event-store"
